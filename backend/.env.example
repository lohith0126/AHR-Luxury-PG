# MongoDB connection string (Atlas or local). Never commit the real .env file.
MONGO_URI=mongodb://127.0.0.1:27017/ahr_luxury_pg
PORT=5000
# Optional: restrict CORS to your deployed frontend URL. Leave empty to allow all.
# CLIENT_ORIGIN=
